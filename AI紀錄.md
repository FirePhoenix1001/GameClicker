```
我目前有兩份專案分開我希望你把它們整合再一起，因為我的KeyboardMacro是使用CLI當介面，所以需要再GUI裡面添加他的分頁，再來是新增一個.spec檔，用於我日後打包成exe檔，工作要分的明確GUI就是負責介面，KeyboardMacro就是鍵盤功能，MouseMacro就是滑鼠功能，我還需要一個main用來啟動程式
```
```
我想要新增一些功能
1.滑鼠錄製：當使用者按下某個鍵盤按鈕時，會開始錄製滑鼠的移動與點擊，直到使用者按下停止鍵，就會停止錄製，然後按下播放鍵就會開始播放錄製的滑鼠移動與點擊
2.鍵盤錄製：當使用者按下某個鍵盤按鈕時，會開始錄製鍵盤的按鍵，直到使用者按下停止鍵，就會停止錄製，然後按下播放鍵就會開始播放錄製的鍵盤按鍵
```
```
我想要刪除和新增一些功能
1.滑鼠功能裡我只想保留滑鼠錄製，並且錄製選項多兩個功能一個是重複次數(如果調整成-1就是重複執行)，一個是滑鼠撥放移動速度
```
```
有一些細節要修改
1.滑鼠錄製與播放"移動到"播放設定"下面，把熱鍵設定刪掉，取而代之的是錄製和停止錄製改成"F10"，播放和停止撥放改成"F9"
```
```
有一些細節要修改
1.重複次數多一個重複無限次的選項可以勾選，預設是勾選的，如果沒有勾選才跑重複次數
2.多一個自動關閉程式的計時，使用者可以自定義幾分鐘後關閉，以及保持開啟的選項可以勾選
3.每當開始錄製、停止錄製、開始播放、停止播放時，都要有通知，和讓windows發出聲響
```
```
有一些細節要修改
1.所有可以讓使用者輸入的文字框在點擊空白處應該要取消選取，而不是停留在輸入狀態
2.系統自動化的保持開啟是預設不勾選，預設分鐘數改成1分鐘
3.重複無限次的勾選框放在重複次數的輸入框的右邊
4.紅色的熱鍵F10和藍色的熱鍵F9刪掉，保留兩個按鈕就好
5.應用程式視窗太長了，變短一些
```
```
有一些細節要修改
1.在播放的時候位置會跟錄製的時候位置不一樣，播放位置會往右偏移
2.刪除鍵盤錄製的功能
3.鍵盤的監測控制幫我改成可以讓使用者自定義監測數量，新增一個按鈕可以新增監測數量，新增一個按鈕可以刪除監測數量，鑑測的按鈕和回饋的按鈕都可以讓使用者自定義
```
```
幫我把鍵盤監測控制的表格對齊上方的"出發按鍵""回饋1""回饋2"以及"延遲"
```
```
有一些細節要修改
1.表格是對齊了，但是"觸發按鍵""回饋1""回饋2"以及"延遲"沒有對齊
2.當新增的按鍵超過視窗高度時，應該要可以上下滑動
```
### 未來添加功能